# Core Data Processing
pandas>=2.1.0
polars>=1.0.0
numpy>=1.24.0
pyarrow>=14.0.0
openpyxl>=3.1.0
pytz>=2023.3
PyYAML==6.0.3

# Configuration Management
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Dashboard
dash>=2.14.0
dash-bootstrap-components>=1.5.0
plotly>=5.17.0
gunicorn>=22.0.0; platform_system != "Windows"
nbformat>=4.2.0
itsdangerous>=2.2.0

# Campbell AI internal API and agents runtime
fastapi>=0.115.0
uvicorn>=0.30.0
openai-agents>=0.3.3,<0.4.0

# Shared conversation sessions across workers/replicas.
# Only needed when CAMPBELL_AI_SESSION_BACKEND=redis; the default in-memory backend
# imports nothing. Kept declared so a horizontally scaled deployment installs it.
redis>=5.0.0

# Utilities
tqdm>=4.66.0

# S3 Interaction (pinned to avoid pip backtracking; must support S3 conditional
# writes (IfMatch/IfNoneMatch on PutObject, used by config/client_services.py) -
# 1.35.0 predates that feature, this pin matches what's verified working)
boto3==1.42.30
botocore==1.42.30
s3fs>=2025.5.0
